configuration:
  externalUrl: https://platform.127.0.0.1.nip.io/workflows

ingress:
  enabled: true
  className: nginx
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
  path: /workflows(/|$)(.*)
  pathType: ImplementationSpecific
