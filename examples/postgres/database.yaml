apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: graphwise-workflows-postgres
spec:
  instances: 1
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: workflows
      owner: workflows
