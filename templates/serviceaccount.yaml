{{- if .Values.serviceAccount.create -}}
{{- $saAnnotations := mergeOverwrite (deepCopy .Values.annotations) .Values.serviceAccount.annotations -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "graphwise-workflows.serviceAccountName" . }}
  namespace: {{ include "graphwise-workflows.namespace" . }}
  labels:
    {{- include "graphwise-workflows.labels" . | nindent 4 }}
  {{- with $saAnnotations }}
  annotations:
    {{- tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
{{- end }}
