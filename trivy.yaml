---
timeout: 5m
exit-code: 1
format: sarif
output: trivy.sarif
scan:
  skip-dirs:
    - examples/**
  scanners:
    - vuln
    - secret
    - misconfig
    - license

misconfiguration:
  helm:
    kube-version: "v1.32.0"

severity:
  - MEDIUM
  - HIGH
  - CRITICAL
